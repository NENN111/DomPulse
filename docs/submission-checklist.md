# Чек-лист сдачи

- [ ] `python -m pytest -q` проходит полностью.
- [ ] `python scripts/export_openapi.py` не меняет OpenAPI неожиданно.
- [ ] `docker compose build` выполняется.
- [ ] `/health` возвращает `200`.
- [ ] `.env`, `.local/`, базы, токены и сертификаты не отслеживаются Git.
- [ ] Проверены привязка, создание, карточка, ответ УК, статусы и подтверждение жителя.
- [ ] Проверены общий инцидент и добровольное присоединение.
- [ ] Для demo выбран ровно один транспорт: webhook или polling.
- [ ] Реальные и модельные интеграции явно названы в README.
